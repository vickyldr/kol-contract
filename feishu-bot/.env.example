# 复制为 .env 并填好（或用 pm2/systemd 设成环境变量）
# 长连接模式：不需要公网域名 / HTTPS / Verification Token / Encrypt Key

# 飞书应用凭证（开放平台 → 你的应用 → 凭证与基础信息）
FEISHU_APP_ID=cli_xxx
FEISHU_APP_SECRET=xxx

# 飞书国内填 feishu；Lark 国际版填 lark
FEISHU_DOMAIN=feishu

# AI 二选一：
# (A) 千问（国内 VPS 推荐，国内可达）。觉得 qwen-plus 不够聪明就换 qwen-max。
QWEN_KEY=sk-xxx
QWEN_BASE=https://dashscope.aliyuncs.com
QWEN_MODEL=qwen-plus

# (B) Claude 中转（国内推荐这个用 Claude）：填中转的【完整 chat/completions 地址】+ key + 模型名。
#     大多数国内 Claude 中转是 OpenAI 兼容格式。填了 RELAY_URL+RELAY_KEY 就优先用它。
#     带教答疑推荐 claude-sonnet-4-6（够聪明、回得快）；要更强改成 claude-opus-4-8 即可。
# RELAY_URL=https://你的中转域名/v1/chat/completions
# RELAY_KEY=你的中转key
# RELAY_MODEL=claude-sonnet-4-6

# (C) Anthropic 原生接口（很少用；多数国内中转走上面的 RELAY_*）
# ANTHROPIC_KEY=sk-ant-xxx
# ANTHROPIC_MODEL=claude-sonnet-4-6
# ANTHROPIC_BASE=https://api.anthropic.com

# ───────────────────────────────────────────────────────────────────
# 让机器人「实时读你自己飞书里的文档」当知识库（你改文档，它下次就知道）。
# 前提：文档必须在【机器人所在的同一个飞书组织】里（个人飞书即可，你是管理员）。
#   公司组织的文档跨组织读不到——这是飞书硬规则，需要公司管理员才行。
# 配置任选其一（也可都填）：
#   - 整本知识库：FEISHU_WIKI_SPACE_ID = 知识库链接里的 space_id（含所有子页面）
#   - 单独几篇云文档：FEISHU_DOC_TOKENS = document_id，逗号分隔
# 还要：① 应用权限里开 docx:document:readonly + wiki:wiki:readonly
#       ② 把文档/知识库「分享」给这个机器人应用（添加为可阅读的协作者）
# FEISHU_WIKI_SPACE_ID=7xxxxxxxxxxxxxxx
# FEISHU_DOC_TOKENS=doccnXXXX,doxcnYYYY
# DOC_REFRESH_SECONDS=300
